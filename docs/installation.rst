============
Installation
============

At the command line::

    $ easy_install plugs-core

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-core
    $ pip install plugs-core
